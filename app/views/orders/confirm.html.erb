<div class="container">
	<div class="row">
		<div class="col-xs-12">

			<h3>注文情報確認</h3>
			<div class="col-xs-8">
			<%= form_for(@cart_item, url: new_order_path(@cart_item), method: :post) do |f| %>
			  <table class="table table-borderd">
				<thead>
					<tr>
					  <th class="col-xs-3 active">商品名</<th>
					  <th class="col-xs-3 active">単価(税込)</th>
					  <th class="col-xs-3 active">数量</th>
					  <th class="col-xs-3 active">小計</th>
					</tr>
				</thead>

				<tbody>
					<% sum = 0 %>
					<% @cart_items.each do |cart_item| %>
					  <tr>
						<td><%= cart_item.item.name  %></td><!-- 商品名 -->
						<td><%= cart_item.item.price_with_tax %></td>
						<td><%= cart_item.quantity  %></td>
						<td><%= cart_item.item.price_with_tax * cart_item.quantity %>
							<% sum = sum +cart_item.item.price_with_tax * cart_item.quantity%>
						</td>
					  </tr>
					<% end %>
					
				</tbody>
			  </table>
			<% end %>
			</div>

			<div class="col-xs-4">
				<table>
					<th class="col-xs-3 active">送料</th>
					<th class="col-xs-3 active">商品合計</th>
					<th class="col-xs-3 active">請求金額</th>
					<td class="col-xs-3"><%= 800 %></td>
					<td class="col-xs-3"><%=  %></td>
					<td class="col-xs-3"><%= %></td>
				</table>
			</div>


<div class="row col-xs-12">
			<th>支払い方法</th>
			<!-- <td><%= @order.customer.payment_method %></td> -->
			<th>お届け先</th>
			<!-- <td><%= @order.customer.address %></td> -->
</div>

<div class="row col-xs-12 col-xs-offset-5">
			<%= link_to '購入を確定する', orders_complete_path, class: "btn btn-primary" %>
</div>
		</div>
	</div>
</div>

